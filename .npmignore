# Source (shipped as dist/ via tsup)
src/
tsconfig.json
tsup.config.ts

# Scratch dirs from wizard
.yuna-deploy/
.yuna-deploy-*/

# Marketing site (deployed separately to yuna.bot, not part of the CLI package)
landing/

# Server build/dev junk (we ship server/src/ and server/package.json only)
server/node_modules/
server/.next/
server/.vercel/
server/.env
server/.env.*
server/dist/
server/*.tsbuildinfo

# Misc
.git/
.gitignore
.github/
PLAN.md
CLAUDE.md
*.log
